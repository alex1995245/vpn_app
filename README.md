# VPN App

Flutter VPN application that converts a SOCKS5 proxy into a full system VPN.

## Features
- Windows + Android support
- SOCKS5 proxy with username/password authentication
- Full system VPN via tun2socks + wintun
- Beautiful dark UI

## Setup
```bash
flutter pub get
flutter run -d windows
```